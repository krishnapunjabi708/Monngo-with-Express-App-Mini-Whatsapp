<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit The Chat</title>
   <link rel="stylesheet" href="/styles_edit.css">
</head>
<body>
    <h4><b>Edit your chat</b> <%= chat._id%></h4>
   <p>This chat was sent from <b><%= chat.from  %></b> to <b><%= chat.to  %> </b></p>
   <form method="post" action="/chats/<%= chat.id_%>?_method=PUT">
    <textarea name="msg" rows="5" cols="50"><%= chat.msg %></textarea>
    <button>Edit</button>
   </form>
</body>
</html>